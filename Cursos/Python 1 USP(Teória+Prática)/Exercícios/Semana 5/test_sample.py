'''def fatorial(x):
    fator = 1
    a = 2
    while x >= a:
        fator = fator * a
        a = a + 1\
    return fator

def numero_binomial(m,n):
    a = fatorial(m) / (fatorial(m - n) * fatorial(n))
    return a
'''
'''def test_one(x, y):
    Soma = x+y
    return Soma

def test_two():
    return test_one(3, 1) == 4'''