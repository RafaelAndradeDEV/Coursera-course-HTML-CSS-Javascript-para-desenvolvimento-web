'''def fatorial(x):
    fator = 1
    lista_inversa = 2
    while x >= lista_inversa:
        fator = fator * lista_inversa
        lista_inversa = lista_inversa + 1\
    return fator

def numero_binomial(m,n):
    lista_inversa = fatorial(m) / (fatorial(m - n) * fatorial(n))
    return lista_inversa
'''
'''def test_one(x, y):
    Soma = x+y
    return Soma

def test_two():
    return test_one(3, 1) == 4'''